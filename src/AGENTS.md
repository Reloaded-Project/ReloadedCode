After code changes, run `verify` from `.cargo/` or `src/.cargo/` before returning.
Review-only rules files (`general.md`, `performance.md`, `documentation.md`) live alongside `verify` in that directory.
